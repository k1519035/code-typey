# Normalise line endings so a checkout on Windows doesn't show every file as
# modified. Without this the working copy is CRLF while the repository holds
# LF, and every diff looks like a full rewrite.
* text=auto eol=lf
